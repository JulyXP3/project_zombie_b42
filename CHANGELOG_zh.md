# 更新日志

## [3.1.5] - 当前版本

- 功能: 新增「陷阱」选项卡(自绘 `trap.png` 图标, 位于生成物品页旁)—— 搜索框 + 全部可生成食物列表(`hungerChange < 0` 自动过滤), 选中点「生成」走陷阱链(需站在已放置的陷阱旁)。
- 功能: **F10** 快捷键触发「重置容器」(与按钮同入口, 仅多人模式)。
- 调整: 「生成物品」页移除「陷阱刷物品」按钮, 「重置容器」按钮移到其位置(文案标注 F10)。
- 调整: 默认开启「对僵尸多重打击」「视野为 360 度」(字段初始化与配置默认值同步改为开启)。
- 调整: 默认开启反侦测三开关 —— 禁止执行默认日志记录器、禁止执行提及作弊的文件、禁止执行含可疑词的文件。
- 移除: 物品置换功能 —— 查证 `AddItemInInventoryPacket` 只有 `processClient`(服务端→客户端方向), 客户端发送被服务端静默忽略; 而 `RemoveInventoryItemFromContainerPacket` 的 `processServer` 会真实删除物品并写服务端 "item" 日志(用户名+坐标+物品), 无可行置换通道, 已全量移除(含三语言翻译与图标)。

### 计划 / 待更新(未实现)

- 容器类物品 / 尸体容器战利品重 roll 深挖(仅完成静态分析, 未实现):
  - 开箱机制确认: `RequestItemsForContainerPacket` → 服务端对未探索容器执行 `ItemPickerJava.fillContainer` 按容器类型表 roll 后推送; 枪箱/军用包等容器物品的表含武器。
  - 待实测 1: 房间容器内的容器物品(针线盒/枪箱等)**不拿空**, 反复重置房间容器, 观察 `fillContainerInternal` 对内嵌容器的 `fillRand` 补货是否可重复刷。
  - 待实测 2: 房内尸体容器(`inventorymale/female`)重置 explored 后反复搜尸, roll 僵尸战利品表(武器/弹药/衣物); 街道尸体因服务端命令 `getRoom()` 空引用会失败, 不在范围。
  - 扩展点: `EtherContainerPOC` 重置循环纳入尸体对象分支, 文档补充"不拿空"用法。

## [3.1.4]

- 修复: 玩家编辑页 3D 头像未设置角色, 导致 `UI3DModel` 渲染时每帧 NPE(改用原生 `setCharacter` 用法)。
- 调整: 小地图「物品」图层开关默认改为**关闭**(此前默认开启)。

## [3.1.3]

- 修复: 物品雷达漏扫尸体身上的物品 —— 尸体 (`IsoDeadBody`) 不在 `getObjects()` 里, 需遍历独立列表 `sq:getDeadBodys()` 并读取其 `getContainer()`。
- 修复: 小地图快捷开关栏与主面板勾选框**双向同步**(统一以 Java 配置标志为唯一事实源), 主面板新增「显示物品」勾选框对齐小地图。
- 修复: Kahlua 解析器不支持表构造字面量直接取下标 (`{...}[k]`), 导致 `UIMovableMiniMap.lua` 加载失败、小地图无法打开; 兼容性检查黑名单新增 `}[` 模式。

## [3.1.2]

- 功能: **物品搜索 + 小地图标记** —— 主面板「生成物品」页选中物品后「在地图上显示」, 扫描玩家周围 48 格(楼层 ±1)已加载区块:
  - 家具/容器内容 (`IsoObject` 容器)
  - 地面散落物品、地上的包(含包内物品)
  - 车辆部件容器(后备箱、座位等)
- 功能: 小地图快捷开关栏(窗口顶部 `我 / 玩家 / 载具 / 僵尸 / 物品`, 白=开/灰=关); 关闭「物品」立即清除标记并停止逐帧开销, 重开自动按上次目标静默重扫。
- 功能: 标记跟随角色移动刷新(走出 5 格且间隔 ≥2 秒静默重扫)。
- 改进: 标记刷新改为事件驱动防抖(背包物品数变化、库存窗口变化后安静满 1 秒重扫), 移除定时器, 空闲零开销。
- 修复: 替换 B42 Lua 虚拟机 (Kahlua) 中不可用的 API —— `next()`、`ISUIElement.getVisible()`、`VehicleParts.getParts()`、`ItemContainer.size()`。
- 加固: 新增 `tests/check_kahlua_compat.lua` 静态检查, 构建前拦截禁用写法。
- 调整: 小地图默认尺寸 256→300; 更新致谢文案。
- 文档: 新增 README / README_zh; 安装方式统一为 build.bat → install.bat。

## [3.1.0]

- 初始版本: 基于 [dei0/EtherHack](https://github.com/dei0/EtherHack) 的 Project Zomboid B42 社区移植版。
