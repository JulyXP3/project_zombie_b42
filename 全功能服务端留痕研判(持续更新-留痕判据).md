# 全功能服务端留痕研判

> 最后更新: 2026-09-21
> 日期: 2026-09-03
> 背景: 「战利品重掷」「陷阱武器链」「钓竿生成失败」三处留痕已在 UI 加红字警告。
> 本轮全量排查其余所有功能在多人服务器上的服务端足迹。
> 结论: 三处必留痕已警示; 两个弱留痕通道(一键启动/冒名聊天)与正常流量同构;
> 其余功能经核查零留痕或已被既有设计规避。

## 一、服务端留痕机制全景 (当前 jar b0bbce05d5 反编译核实)

### 1.1 日志通道 (与反作弊无关, 改配置关不掉)
| 通道 | 触发 | 落盘 |
|---|---|---|
| cmd 日志 | 每条 `receiveClientCommand` (GameServer:2135-2137), 默认 ClientCommandFilter 只滤 vehicle 模块 (ServerOptions:137) | `<logs>/<时间戳>_cmd.txt`, 含用户名+命令+坐标 |
| map 日志 | `AddExplosiveTrapPacket.processServer` (AddExplosiveTrapPacket:132-133) | `<时间戳>_map.txt` + console, 含用户名+物品+坐标 |
| chat 日志 | `ChatServer.processMessageFromPlayerPacket` (ChatServer:176,206) 每条消息两行 | `<时间戳>_chat.txt` |
| 统一异常 | 包处理抛异常 (GameServer:1501-1508) → Error 级 + 堆栈 + `AntiCheat.PacketException.act` | console + 日志文件 |
| userlog | 反作弊 act() 触发时写数据库 (AntiCheat:168) | `/userlog` 可查 + 管理员聊天实时广播 (AntiCheat:127) |

### 1.2 反作弊挂载 (PacketSetting.anticheats 注解, 全量枚举)
| 包 | 挂载 | 服务器默认策略 (EnumServerOption(owner,name,numValues,defaultValue) 第4参) |
|---|---|---|
| PlayerPacketReliable/Unreliable (移动) | Power, Speed, **NoClip**, Player | Speed=**2 Kick**; NoClip=4 Disabled |
| PlayerHitZombie/Player/Animal/Object/Square | HitDamage, HitLongDistance, HitWeapon, Safety | 全部 = **2 Kick** (AntiCheatHit:168) |
| VehiclePhysics / VehicleHit* | Speed (+HitDamage/ShortDistance/HitVehicle) | Speed=**2 Kick** |
| 其余全部包 (SyncPlayerFields/SyncItemFields/SyncHandWeaponFields/SyncVisuals/FishingAction/AddExplosiveTrap/Chat/NetTimedAction...) | None | 无 |

阈值细节:
- Speed: movable.getSpeed() > 20 (AntiCheatSpeed)
- **(2026-09-15 更正, 一百零一 取证)**: Speed 阈值分两档 (`AntiCheatSpeed.java:33`) — 步行玩家 = 20;
  **载具 (物理包/车上乘客, `movable.isVehicle()`) = `ServerOptions.SpeedLimit` 默认 70 格/秒** (且采样窗
  dt 实际可达 ~1150ms 而非整 1000ms — 物理包周期 150ms, 位移预算判据详见 C 设计方案 一百零一)。
  上行 "Speed>20" 仅对步行档成立。
- HitDamage: damage > 100.0 (AntiCheatHitDamage)
- HitLongDistance: distance > relevantRange×8×1.2 (数百格, 攻击距离延长够不着)
- XPUpdate: 周期校验 xpDelta > 1000×mult (AntiCheatXPUpdate, 非包挂载)
- 触发链: act() → console+日志+userlog+管理员广播 → counter≥max 即 Ban(1)/Kick(2); PacketException/NoClip 默认 4=Disabled

## 二、逐功能判定

### A. 必留痕 (已加 UI 红字警告)
| 功能 | 留痕 | 证据强度 |
|---|---|---|
| 战利品重掷 | cmd.txt 每容器一行; vanilla 客户端唯一调用点被 `if ISLootZed.cheat or isAdmin()` 门控 (ISInventoryPage.lua:1387), 普通玩家身份出现即铁证 | 极高 |
| 陷阱武器链 | map.txt + console 双日志带用户名/物品/坐标 (放置即触发) | 极高 |
| 钓竿生成失败 | GameServer 统一 catch → Error+堆栈; 默认 PacketException=Disabled 不进 userlog, 但日志在 | 高 |

### B. 留痕但与正常流量同构 (弱证据)
| 功能 | 说明 |
|---|---|
| 一键启动 (vehicleInstantStart) | `vehicle.startEngine` 进 cmd.txt, 但 vanilla 点火走同一命令 (ISStartVehicleEngine→tryStartEngine→sendClientCommandV, BaseVehicle:6817), 日志行不可区分 (cmd 日志不记录参数)。风险点: ServerSyncBlocker 的"周期补发"会产生重复行 (正常玩家每点火只发一次); 以及管理员目击无钥匙启动。 |
| 冒名聊天 (sendChatAs) | 每条消息进 chat.txt (正常聊天同样); 日志记录的是伪造作者名, 不指向发送者。风险点: 管理员对照在线名单/被冒名者举报。 |
| **尸体生成 (AddCorpseToMapPacket 伪造, 2026-09-04 新增)** | 服务端 item 日志一行: `<IP或SteamID> "<用户名>" corpse +1 <x>,<y>,<z>` (`<logs>/<时间戳>_item.txt`, AddCorpseToMapPacket.java:83; 不进 console/userlog)。**归因直接**: 行内自带发送者用户名+IP。**定罪间接**: vanilla 抓取尸体物品 (ISGrabCorpseItem→pickUpCorpseItem→tryAddCorpseToWorld) 走同一包同一日志, 单行形态与频次均不可区分 (合法玩家杀尸群后搬运一堆尸体 = 同样高频 corpse +1); 配对破绽 (vanilla 必有 `container -1 [尸体物品]` 前置行, 伪造没有) 只属理论取证手段。无反作弊。**2026-09-05 用户决策: UI 不加红字警告** (可区分度过低, 不值得警示); 文档分析保留。详见「VHS获取经验与尸体物品伪造(已实施完成).md」§7.2。 |

### C. 零留痕 (正常操作全程无服务端日志、无反作弊挂载)
| 功能 | 依据 |
|---|---|
| 配方学习 (SyncPlayerFields PF_Recipes) | parse 即写入零校验, processServer no-op, 无日志 |
| 弹药篡改/无限弹药/退弹刷弹药 | SyncItemFields/SyncHandWeaponFields 正常路径零日志 (仅 itemId 找不到时 error); HitWeapon 的 isAmmoExceeded 只判 ≤0, 篡改后绕过 |
| 钓竿生成 (成功时) | FishingAction 原始包, 服务端 AddItem 权威操作无日志 |
| 衣物修复/填充皮革 | SyncVisuals 零校验零日志 (SyncVisualsPacket:96-136) |
| 上帝模式/全 stats 开关 | stats 随 PlayerPacket 上行照收; AntiCheatPlayer 只查 playerId 归属, AntiCheatPlayerUpdate 只查 30s 超时, 数值零校验 |
| noclip 穿墙 | AntiCheatNoClip 服务器默认 Disabled (服主手动开启才 Kick) |
| 技能等级/XP 设置 | setPerkLevelDebug 本地 + protectedSkillLevels 拦下行纠正包, 不上行 |
| 秒杀 | Hit.set 钳 ≤100 卡在 AntiCheatHitDamage 阈值之下 (damage>100 才触发); 伤害三件套设计已在包络内 |
| 只爆头/百发百中/无后座/不卡壳/攻击速度/攻击距离 | 命中判定本地化, 命中包字段在服务器包络内; HitLongDistance 阈值 relevantRange×8×1.2 极高 |
| 玩家传送 (safePlayerTeleport) | 每步 1 格 + 同步 lastX/Y/Z (单包速度=0) + 逐步 sendPlayer, 规避 Speed>20 |
| 物品生成页 (spawnItem/giveItem) | 纯客户端本地 AddItem/AddWorldInventoryItem, MP 下服务端不认 (零足迹也零效果) |
| 视觉类 (夜视/全亮/360/ESP/雷达/名牌/僵尸色) | 纯客户端渲染 |
| TimedActionCheat/多杀/暴击/无限负重/无限耐久 | 本地或包络内 |

> **(2026-09-15 补录, 详见 §三C)**: 本表判定随后续取证有增补 — ① **临时武器**不入"零留痕"
> (每枪命中包被服务端 isConsistent 拒收 → console WARN 可溯源 + **零服务端伤害**, §三C.1);
> ② **VHS教学/电台经验**新增弱留痕 (服务端原版 `Recipe AutoLearned` LOG + knownRecipes 永久入档,
> §三C.2); ③ **汽车传送 (C3)** 经实测与取证确认仍属零留痕 (§三C.3)。

### D. 条件触发面 (默认 Kick, 既有设计已规避, 列此备查)
| 反作弊 | 阈值 | 撞上的 hypothetical 功能 | 现状 |
|---|---|---|---|
| Speed (Kick) | 速度>20 | 瞬移/载具加速 | safePlayerTeleport 已规避; 无载具加速功能 |
| HitDamage (Kick) | 伤害>100 | 秒杀 | 已钳值 |
| HitWeapon (Kick) | 弹药≤0 射击 | 无限弹药 | 篡改后恒>0 绕过 |
| XPUpdate (Kick) | 单次 xpDelta>1000×mult | 经验倍率类 | 未实现 (研判不可用, 亦会撞此反作弊) |

> **(2026-09-15 补录)**: "无载具加速功能"已过时 — **C3 汽车传送** (连人带车跳传) 已实施, 其载具
> Speed 撞击面由**位移预算 + 停稳门**规避 (任意 ≤1500ms 窗内载具位移 ≤ 0.6×限速 → 任何采样窗读数
> 恒低于阈值; 一百零一 三次实测零触发, 详见 §三C.3 与 C 设计方案 一百零一)。

## 三、结论

1. **必留痕仅三处** (重掷/陷阱武器/钓竿失败), 均已 UI 红字警示。
2. **弱留痕三处**: 一键启动与冒名聊天——进常规日志但与正常流量同构, 无法从日志行本身定罪; 前者注意周期补发的重复行模式, 后者注意被冒名者在场时可对质。**尸体生成 (2026-09-04 新增)** — item 日志一行 corpse +1 直接记名 (用户名+IP), 但 vanilla 合法搬运尸体同款同频, 可区分度过低, **UI 不加警示** (2026-09-05 用户决策); 配对分析仅理论取证面, 详见电台/尸体研判 §7.2。
3. **其余全部功能**经包挂载与日志双面核查为零留痕 (默认配置); 唯一需持续留意的是服主手动调高反作弊等级 (Speed/Hit 系从默认 Kick 无变化; NoClip/PacketException 从 Disabled 改 Log/Kick/Ban 会新增触发面)。
4. 附带修正既有认知: B42 反作弊默认值中 **Speed/Hit/XP/Player/SafeHouse/Checksum/Safety/Permission 均为 Kick**, 仅 **PacketException 与 NoClip 默认 Disabled** (此前把 antiCheatSpeed/antiCheatHit 的 numValues=4 误读为默认关)。
5. **(2026-09-15 补录)**: 结论 3 的"零留痕"清单随后续取证有四处增补/更正 — ① Speed 阈值分档 (§1.2 更正); ② 临时武器 = console WARN 留痕 + 零服务端伤害 (§三C.1); ③ VHS教学 = 原版配方自动学习 LOG + 永久档案 (§三C.2); ④ 汽车传送 (C3) = 零留痕定案 (§三C.3)。

## 三B、2026-09-13 补录 (PienZ DLL 逆向校验轮的新知识, `analysis/DLL分析/PienZ-DLL逆向分析(第八版-源码已核对).md`)

### 3B.1 cmd 日志的默认过滤器语义 (修订 §1.1 "改配置关不掉"的粒度)

`GameServer.receiveClientCommand` (`GameServer.java:2135-2137`) 写 cmd 日志前过一道
**`ClientCommandFilter`** (`ServerOptions.java:137`, 默认
`-vehicle.*;+vehicle.damageWindow;+vehicle.fixPart;+vehicle.installPart;+vehicle.uninstallPart`),
`CCFilter.matches` 支持 `*` 通配、`+`=记录/`-`=抑制 (`GameServer.java:4127-4139`):

- **`vehicle.*` 默认整模块不记**, 但上述 4 条被白名单显式记录 ⇒
  - 一键启动 (`vehicle.startEngine`) = 被抑制档, **默认不记** (比本文件 §二-B 原判定更乐观);
  - ~~修车逐件 `vehicle.fixPart`~~ = **白名单内必记** ⇒ **B1 已修** (2026-09-13, `EtherVehiclePanel`
    修理默认改走 `ISRepairLightbar` 原版计时动作, 直发路线降级为显式勾选且三语 UI 标注"会留日志");
  - 加油 (`vehicle.setContainerContentAmount`) = 被抑制档, 不记。
- 服主改过滤器可翻转以上判定; 本研判按默认值。
- 服务端 Lua 侧 `noise()` 仅 `getDebug()` 时 print (`VehicleCommands.lua:6-8`), 默认无输出。

### 3B.2 userlog 类型表 (反作弊/系统侧落库, `/userlog` 可查 + 管理员实时广播)

| 类型 | 触发 | 证据 |
|---|---|---|
| SuspiciousActivity | 任一 AntiCheat.act() 升级路径 | `AntiCheat.java:168` |
| Kicked / Banned | 反作弊踢/封路径; BanSystem; 管理员 KickUserCommand | `AntiCheat.java:174/182`, `BanSystem.java:36` |
| LuaChecksum | ChecksumPacket 校验失败 (受默认策略管, Disabled 则只不写) | `ChecksumPacket.java:248` |
| **DupeItem** | `AddInventoryItemToContainerPacket` 服务端判"物品 ID 重复入库" | `AddInventoryItemToContainerPacket.java:126-138` |
| WarningPoint | 警告点系统 | `ServerWorldDatabase.java:1551` |

### 3B.3 物品入包路线的留痕对照 (新增功能选型用)

| 路线 | 服务端行为 | 留痕 |
|---|---|---|
| `AddInventoryItemToContainerPacket` (客户端→服务端) | 建物入库; 有 **DupeItem** 检测 | item/userlog (命中重复 ID 时) |
| `InvMngGetItem` (LoginOnServer 门) | **只把包原样转发**给包内 onlineID 玩家自己的连接, 服务端不建物不校验 (`GameServer.java:1524-1540`); 目标客户端 `loadItem`+`addItem` 落**本地** (`GameClient.java:662-674`) | 服务端零日志; 物品是否落档取决于后续同步 (待 B2 实测) |
| 钓竿链/陷阱链 (我方已实施) | 服务端权威 AddItem | 无日志, 产物合法实例 |

### 3B.5 原版动作审计通道 ClientActionLogs (2026-09-13 二轮实测发现)

用户服务器 cmd.txt 出现 `"july" ISLogSystem.writeLog @ x,y,z` —— **原版动作审计**: `shared/Logs/ISLogSystem.lua`
在**每个计时动作完成时** (`ISBaseTimedAction:perform` 收尾) 调 `logAction`, 动作类型命中服务端选项
**`ClientActionLogs`** (默认 `"ISEnterVehicle;ISExitVehicle;ISTakeEngineParts;"`, `ServerOptions.java:138`) 时,
客户端发 `ISLogSystem.writeLog` 命令, 服务端 `writeLog("ClientActionLog", text)` 落
`<logs>/<时间戳>_ClientActionLog.txt` (行内容 = [steamID][动作类型][用户名][坐标][额外数据])。

- **含义**: cmd.txt 里每行 writeLog = 玩家完成了一次**上车 / 下车 / 拆引擎部件** (vanilla 正常玩家同款, 零额外暴露);
  服主可改 `ClientActionLogs` 增删审计的动作类型 (string.match 子串匹配)。
- **与我们的功能**: ① B1 计时修车的**下车** (`ISVehicleMenu.onExit` → `ISExitVehicle`) 会产生一行 (与正常下车同构);
  ② **C1 远程上车不产生** —— 直接发 VehicleEnterPacket, 不走 ISEnterVehicle 计时动作; 正常玩家上车反而有一行
  (ISVehicleMenu.onEnter 排队 ISEnterVehicle) ⇒ "有上车行为但无 ISEnterVehicle 行" 属理论配对取证特征 (极弱,
  需服主专门对照两份日志, 现实中无人这么做); ③ 我方代码不调用 ISLogSystem (LuaCompiler 黑名单里反而有该词)。

### 3B.4 本轮新研判的包面 (C/D/F 类新功能共用结论)

`VehicleEnterPacket` / `SyncIsoObject` 均 `requiredCapability=LoginOnServer`、**无 anticheat 挂载、不进 cmd 日志**
(`PacketTypes.java:367/479`); 服务端处理为权威动作后 `sendToClients` 广播 —— 按 §一 归"零留痕 (弱证据: 行为同构性)",
分别见 `analysis/DLL分析/C-传送与载具-设计方案(待实施).md` 与 F 对照评估。*(2026-09-15 注: 该文件已更名 `C-传送与载具-设计方案(C1-C3已实施).md`。)*

## 三C、2026-09-15 补录 (临时武器 isConsistent 拒包链 / VHS教学配方自动学习日志 / C3 汽车传送留痕定案)

### 3C.1 临时武器 (EtherTempWeapon) — 每枪两条 console WARN + 零服务端伤害

取证 (2026-09-15, 用户自建服 console 日志驱动):
- 客户端每次开枪上行 `AttackCollisionCheckPacket` + `PlayerHit*` (Square/Zombie/Player/...);
  服务端 `PacketTypes.onServerPacket:677-681` **先过 `isConsistent`、后进反作弊** — `Weapon` 字段在
  服务端按 `character.getInventory().getItemWithID(id)` 解析 (`Weapon.java:38-39`), 临时枪只在客户端
  本地生成+持手 (不入背包、零同步) → 服务端解析 **null** → `weapon.isConsistent = (weapon != null)`
  为假 (`Weapon.java:62-64`) → `logInconsistentPacket` 打 **WARN** console 行 (`INetworkPacket.java:59-61`)
  → `packet.sync + return`, **包被丢弃、不执行 processServer**。
- **留痕评估**: WARN 行只含包名 + 字段 JSON (无用户名/SteamID), 但含**精确坐标** (wielder positionX/Y、
  目标格、tracer 端点) + `playerIndex` — 服主交叉比对当刻玩家位置即可溯源; 且正常玩家真枪永远不产生
  该行 → 出现即"有人在用服务端没有的武器开火"的指纹。
- **效果评估 (重要更正)**: 包在全部反作弊之前就被丢弃 → **MP 下临时武器打不出任何服务端伤害**
  (纯本地表演: 枪声/弹道/动画本地可见, 僵尸血量/死亡是服务端权威结算, 不会发生)。此前"临时武器
  零上行"的表述**不成立** — 命中包确实上行, 只是全部被拒收。
- 归类: 弱-中留痕 (console WARN, 与正常流量**不同构** — vanilla 真枪无此行; 无用户名但可坐标溯源;
  代价面 = 每枪 2 条刷屏)。

### 3C.2 VHS教学 (RadioXp 电台经验) — 配方自动学习的原版 LOG + 永久档案痕迹

取证 (2026-09-15):
- 链: 伪造 WaveSignalPacket → 服务端官方 addXp 账本 → 技能升级 → 服务端 Lua `XpUpdate.levelPerk`
  (`XpUpdate.lua:202-204`) → `getScriptManager():checkAutoLearn(owner)` →
  `CraftRecipe.checkAutoLearnAnySkills/AllSkills` (`CraftRecipe.java:881-903`) → 技能达标配方自动学会,
  服务端 console 打 `LOG : General Recipe AutoLearned AnySkills/AllSkills - <配方名>`。
- **评估**: 原版机制 (任何玩家任何方式升级都会触发), **LOG 级 — 非 WARN/非反作弊**, 行内无用户名;
  但一次批量 ~40 条同帧 = "技能批量跳级"指纹 (正常玩家游玩升级只会零星触发); 学会的配方**永久写入
  角色存档** (`IsoGameCharacter.java:5053-5055` save knownRecipes) — 服主查玩家档案可见"全部配方已学",
  长期可查。
- 归类: 弱留痕 (与正常升级日志同构, 但"批量模式 + 永久档案"两面有别于正常玩家)。

### 3C.3 汽车传送 (C3) — 零反作弊留痕定案 (一百零一 实测 + 一百零五 取证)

- **一百零一 (实测)**: 位移预算 + 停稳门落地后, 用户自建服三次长传 (78.5/393.5/366.8 格) 全程
  console 搜 `Anti-cheat`/`Kick`/`Kicked`/`Banned` **零命中** — 汽车传送**不被服务端记录、不触发
  反作弊** (C 设计方案 一百零一 实测结案)。
- **一百零五 (取证, 方案A 抬高落下)**: 每跳抬升物理体垂直落下 — 抬高是垂直位移, `SpeedChecker.set`
  只收二维 x/y (`NetworkCharacterAI.java:350-362`) → 不入速度采样, **可检测性不变 (仍不被记录)**;
  落点在水面时车沉到河床 (水格无物理形状 — `IsoChunk.calcPhysics` 不为水格生成 PhysicsShape,
  支撑来自河床地形 mesh) 同样零服务端日志。
- 归类: §二-C 零留痕 (车辆物理走原生 VehiclePhysics 流, 与正常驾驶同构; 停稳门保证起跳采样窗内
  只剩单跳位移, 预算不变式保证任意窗位移 ≤ 0.6×限速)。

## 三D、2026-09-21 补录 (一百二十九): item 日志 `corpse +1` 行全解 (用户实测提问)

用户实测: 服务端 `2026-09-21_02-13_item.txt` 出现
`[21-09-26 02:53:51.272] 76561198989831730 "july_0" corpse +1 7817,11286,0.`

### 3D.1 出处与归因 (能直接定位到人吗 → 能)

`AddCorpseToMapPacket.processServer` (`:83`, 在 `if (GameServer.server)` 分支内):
```java
LoggerManager.getLogger("item").write(connection.getIDStr() + " \"" + connection.getUserName()
        + "\" corpse +1 " + this.x + "," + this.y + "," + this.z);
```
**服务端在处理"客户端发来的加尸体包"时写这一行** —— `getIDStr()` = **SteamID64**, `getUserName()`
= 用户名, 加坐标。**可确定性归因**(不是"凭空出现一具尸体", 而是"这个连接请求加了一具尸体")。
客户端无法删除: 写日志的是服务端代码 (红线: 不改服务端文件), 客户端侧任何补丁都改变不了它。

### 3D.2 同一个 item 日志文件只有三个写入点 (全量枚举)

| 写入点 | 行格式 | 触发方 |
| --- | --- | --- |
| `RemoveItemFromSquarePacket:124` (`connection != null` 分支) | `floor -1 x,y,z [type]` | 客户端把**地面物品**移走 |
| `RemoveInventoryItemFromContainerPacket:124` | `container -N x,y,z type` | 客户端把**容器里的物品**移走 |
| `AddCorpseToMapPacket:83` | `corpse +1 x,y,z` | 客户端请求**在世界里生成一具尸体** |

⇒ 该文件在**正常游戏里本来就有大量行** (取物/丢物都写), 所以"文件里有我的 ID"本身不构成异常;
异常点在**动词** (`corpse +1`) 与**频次**。

### 3D.2b 三条写入点"在 MP 里究竟谁碰得到" (一百四十四, 用户实测"只有尸体生成会记录"驱动)

用户实测: 那份**服务器** `_item.txt` **只有我方「尸体生成」会写**, 正常玩法(捡/丢物品)一概没有。
逐条核实三条写入点的**客户端发起者**, 结论是用户的观察**符合代码**:

| 行 | 服务端写入条件 | 客户端发起者 (全量核实) | MP 下正常玩法会不会写 |
| --- | --- | --- | --- |
| `corpse +1` | `AddCorpseToMapPacket.processServer` (`GameServer.server` 分支) | 只有**客户端** `addCorpse(body,false)` ⇒ `tryAddCorpseToWorld` (丢下尸体物品/拾起人类尸体) 或伪造尸体 | **只有伪造**(服务端自建尸体走 `GameServer.sendCorpse`, 不写) |
| `floor -1` | `RemoveItemFromSquarePacket.removeItemFromMap(connection≠null)` | `ISGrabItemAction` (`ISWorldObjectContextMenu.lua:1458/1473/1490` 与觅食图标) / `destroyTile` (`:1952`) / 调试工具 `ISRemoveItemTool` | **几乎不会**: `onGrabWItem` 在 `isClient()` 时**改走普通搬运** (不发该包), `destroyTile` 在 MP 走 `sledgeDestroy`, 调试工具非玩法 ⇒ 普通捡取=容器转移, **不写** |
| `container -N` | `RemoveInventoryItemFromContainerPacket` 同一 `getLogger("item")` 调用 | `ISBuildUtil` (建造消耗) / `ClientCommands.lua` (服务端 Lua) / 调试工具 | **服务端 Lua 侧发起** (建造/合成消耗等), 与"玩家捡取"无关; 若要看它, 试 **建造/制作时消耗背包物品** |

**⇒ 结论 (对红队重要)**: 在那台服务器上, **那份 `_item.txt` 基本等于"作弊绊线"** —— `corpse +1` 的
唯一现实来源就是"客户端提交尸体"(我方功能或同类伪造), 正常玩法不会产生它。
要验证"文件是活的", 最省事的合法触发是**在 MP 里建造/制作一次(消耗背包物品)**, 看是否出现 `container -N`
(该链由服务端 Lua 发起); 而"捡了又丢"这类操作**本就不会写任何行** (用户此前那轮对照测试即此)。

### 3D.3 原版也会产生 `corpse +1` 的行为 (掩护面)

`IsoGridSquare.tryAddCorpseToWorld(item,x,y,visible)` (`:5492-5508`): 把背包里的**"尸体"类物品
放到地上** → `addCorpse(dead, false)` → 客户端发 AddCorpseToMapPacket → **同一行**。
"尸体物品"在 B42 包括**动物尸体**(老鼠/鸟等可携带)与人类尸体物品 —— 即**打猎后把动物尸体丢地上**
就是一次合法的 `corpse +1`。
**反例 (不产生该行)**: 僵尸被杀的尸体由**服务端**创建 (`addCorpse(body, bRemote=true)`,
`GameServer.sendCorpse`), 不走客户端包 → 我方碾杀、正常击杀都不写这一行。所以该行永远等于
"**某个客户端主动要求加尸体**", 合法解释基本只剩"丢下尸体物品"这一种。

### 3D.4b 正常玩法里"什么操作会留下这一行" (一百四十一 重查定案, 可照做复现)

> **结论现状 (2026-09-21)**: 代码层面能走到服务端 `corpse +1` 写入点的**客户端提交**有三条 (见下表),
> 但**实测只复刻出我方「尸体生成」这一条** —— 用户按"生成 CorpseMale → 拖到地面"操作无日志, 随后的
> 对照测试又证明"普通捡起/丢下物品"本身一行都不写 (MP 下走普通容器转移、不发包), 所以"文件是活的"
> **不能**用捡/丢来验证 (要用**建造/制作**看 `container -N`)。下表按"代码通路 / 实测状态"两列看 ——
> 唯一的**普适**代码通路是 **把尸体物品「丢下」(Drop) 到地面**, 其余两条各有前提:

**唯一普适通路: 丢下 (Drop) 尸体物品到地面 —— 人与动物尸体都算**

| 步骤 | 代码事实 |
| --- | --- |
| 1. 物品栏里**右键尸体物品 → 「丢下」**(`ContextMenu_Drop`, `ISInventoryPaneContextMenu.lua:634/642` → `onDropItems:3815` → `dropItem:3836`); 或直接把它拖到物品栏界面的**地面区域** (同样是 floor 目的地) | 建成 `ISInventoryTransferAction`, 目的地 = floor 容器 |
| 2. 动作执行 → 目的地是 floor | `ISInventoryTransferAction:transferItem` → `ISTransferAction:transferItem` (`shared/TimedActions/ISTransferAction.lua:104-112`): `dropSquare:AddWorldInventoryItem(item, dropX, dropY, dropZ)` —— **由客户端执行** |
| 3. `AddWorldInventoryItem` (`IsoGridSquare:5425-5427`) | `IsoDeadBody corpse = this.tryAddCorpseToWorld(item, x, y);` —— **物品是尸体**(`isHumanCorpse()`/`isAnimalCorpse()`)才走这条 |
| 4. `tryAddCorpseToWorld` (`:5492-5508`) | 造出世界尸体 → `this.addCorpse(dead, false)` → `IsoGridSquare.addCorpse` (`:2649`) 首分支 `if (GameClient.client && !bRemote)` → **发 AddCorpseToMapPacket** |
| 5. 服务端 | 写 `*_item.txt`: `[时间] <SteamID> "<用户名>" corpse +1 x,y,z`(坐标 = 落点方格) |

**另两条"有前提"的通路**:

| 通路 | 前提 | 是否写日志 |
| --- | --- | --- |
| 右键**人类**尸体物品 → 「拾起尸体」(`ISGrabCorpseItem` → `IsoGameCharacter.pickUpCorpseItem:7887`) | **只对 `isHumanCorpse()`**(`CorpseMale`/`CorpseFemale`)出现; 右键菜单 `ISInventoryPaneContextMenu.lua:3917/3933/3953` 有 `isHumanCorpse()` 分支 | ✅ 写(客户端执行 → 同一条包) |
| 动物尸体的「搬运」/「屠宰」 | —— | ❌ **不写**: 搬运 = 普通容器间转移; 屠宰 = 屠宰动作, 都不经过 `AddWorldInventoryItem` (这正是用户实测看不到日志的原因) |

**复现失败时的四点自查 (一百四十二, 用户按"生成 CorpseMale → 拖到地面"复现无日志后补)**

代码侧结论先说清: 这条路**必发** AddCorpseToMapPacket ——
`AddWorldInventoryItem` (`IsoGridSquare:5425-5427`) 第一行就是 `tryAddCorpseToWorld(item,…)`，
**尸体情况直接 `return item`（不再走任何"交给服务端创建"的延迟逻辑）**；`tryAddCorpseToWorld` 里
`addCorpse(dead,false)` 命中 `if (GameClient.client && !bRemote)` → 发包含 `packet.set(this, body)` 的包。
⇒ 只要**客户端执行到了这条**，包就发出去了；服务端收到就写日志。所以复现不出来只有四种可能:

| # | 自查 | 判据 |
| --- | --- | --- |
| ① | **操作是否真是"丢下"** — 请用**右键 → 「丢下」**(`ContextMenu_Drop`)，不要用拖拽 (MP 下拖拽很容易落到旁边的容器/背包而不是地面) | 丢下后**你脚边应出现一具尸体**（能看见的 IsoDeadBody）；若物品只是进/出了某个容器 → 走的是普通转移, 不写日志 |
| ② | **日志文件是不是"活的"** — 做一次对照: **建造/制作一次**(消耗背包里的一件物品, 如钉一块木板/做一个配方) | 同一个 `*_item.txt` 里**必须出现** `container -N x,y,z [物品]` 行；出现 = 文件是当前的、logger 正常。**注意: 普通捡起/丢下物品在 MP 下一行都不写** (走普通容器转移, 不发包; 2026-09-21 用户对照测试已证实), 拿它当对照只会得出"文件坏了"的错误结论 |
| ③ | **是不是看错文件/看错机器** — 服务端 item 日志按**服务器启动时刻**命名 (`logs/YYYY-MM-DD_HH-MM_item.txt`)，每次重启换新文件；本地自建服要进**服务端目录**的 `logs/` 看 | 文件 mtime 与当前会话一致；`tail -f` 能实时看到新行 |
| ④ | 三项都对却仍无 `corpse +1` | 说明服务端**没收到**该包 (被拦/版本漂移/服务端侧 mod) —— 把 ①的现场(尸体有没有出现) + 客户端 `console.txt`/`modcore_*.log` + 服务端最新 `*_item.txt` 一并回传 |

> 附加判据: 我方「尸体生成」那条路 (客户端日志必打 `[CorpseSpawn] forged corpse uploaded …`) 与
> 上面的原版路**共用同一个服务端写入点** —— 两条**都不写**时, 问题几乎必然在①(操作)/②③(日志venue)；
> 只有其中一条不写时才需要查该条包链本身。

**item 日志文件在哪、怎么看"活的那份" (一百四十三, 用户对照测试"捡了又丢也没行"后补)**

用户做了最关键的对照测试 —— **随便捡一件地面物品再丢下, 服务端 `_item.txt` 里连 `floor -1` 都没有**
⇒ 不是尸体路径的问题, 是**看的那份文件没有在被写**。机制 (`zombie/core/logger/ZLogger.java:31-40`):

```java
this.file = new File(LoggerManager.getLogsDir() + File.separator
        + ZombieFileSystem.getStartupTimeStamp() + "_" + name + ".txt");
```

两条推论:

1. **文件名前缀 = "写日志那个进程的启动时刻"** (`getStartupTimeStamp()`)。同一个 `Zomboid\Logs\`
   目录里会同时存在**客户端进程**与**服务器进程**两份 `*_item.txt` (前缀不同);
   而全部写入点都在服务端分支 (`AddCorpseToMapPacket.processServer` 的 `GameServer.server` 分支、
   `RemoveItemFromSquarePacket` 的 `connection != null` 分支) ⇒ **内容只出现在"服务器进程那份"**,
   客户端那份通常是**空的** —— "什么都没有"极可能是在读空的那份, 或读了几小时前那次启动的旧文件。
2. **日志目录** = 该进程 cache dir 下的 `Logs` (`LoggerManager.getLogsDir()` =
   `ZomboidFileSystem.getCacheDirSub("Logs")`); 自建服默认也在 `%USERPROFILE%\Zomboid\Logs\`;
   若服务器用 `-cachedir` 指到别处, 那份文件就在那个目录的 `Logs` 里。

**取"活的那份"的可靠办法** (PowerShell, 按修改时间排序取最新):

```powershell
Get-ChildItem -Path "$env:USERPROFILE\Zomboid" -Recurse -Filter "*_item.txt" -ErrorAction SilentlyContinue |
  Sort-Object LastWriteTime -Descending | Select-Object -First 5 LastWriteTime, Length, FullName
```

**复测顺序**: 先跑上面的命令找到**最新那份**(mtime 与当前会话一致、Length 在涨) → `tail -f` 盯着它 →
再捡/丢一件地面物品 → 应立刻出现 `floor -1 …`; 再走"丢下尸体物品" → 应出现 `corpse +1 …`。
若最新那份仍然常空, 把上面命令的**完整输出**贴回来, 一眼能看出是"文件选错"还是"服务端确实不写"。

**怎么弄到一个尸体物品 (前提物)**: 人类尸体物品 `Base.CorpseMale/CorpseFemale` 是**稀有战利品**
(`ProceduralDistributions` 某些容器 junk 表 1%、车后备箱/座位杂物 0.1%); 动物尸体物品
`Base.CorpseAnimal` 打猎/屠宰小动物后获得。**测试最省事的办法 = 用我方「计时生成」直接造一个**
(`Base.CorpseMale` 或 `Base.CorpseAnimal`)。注意: 造出来之后**丢下它复刻不出 `corpse +1`**
(用户实测, 见本节开头"结论现状"); 要日志就直接走我方「尸体生成」。

**为什么"服务端自己生成尸体"不写这行**: 服务端侧造尸体走 `tryAddCorpseToWorld` 里的
`if (GameServer.server) { GameServer.sendCorpse(dead); }` 分支 —— 服务器直接广播, **不经过
`AddCorpseToMapPacket`**, 因此 `corpse +1` 这行**只可能是"某个客户端请服务端加尸体"**;
客户端侧现实中能触发它的只有**伪造提交**这一类 (我方尸体生成 = `core/CorpseSpawnAPI.java`) ——
普通搬运/丢下在 MP 下不发这条包 (用户对照测试已证实)。

### 3D.4c 自测"到底有没有写日志"的排查顺序 (一百三十八 用户提问后补)

用户实测反馈"我明明生成了尸体却没日志" —— 逐级排查看哪一环断掉:

1. **客户端有没有真的发包**: 看客户端日志 `modcore_*.log` 里有没有
   `[CorpseSpawn] forged corpse uploaded with Nx <物品>` —— **没有这行 = 功能压根没跑到**
   (最常见: 不在多人服 / 面板按钮没点中物品 / 面板显示失败);
   **这行的触发操作** (2026-09-21 一百四十 用户追问后写明):
   `战利品面板 → 模式切到「尸体生成」→ 在物品列表里**点中**一个物品 → 数量填 1~100 → 点「生成」`
   → Lua `EtherCorpseSpawn.trigger` → Java `corpseSpawnPrepare(item, count)` 成功即打这行
   (代码: `core/CorpseSpawnAPI.java` 末尾的 `Logger.printLog("[CorpseSpawn] forged corpse uploaded with …")`)。
   前置: 必须**已连入多人服务器** (`isMultiplayer()` 为假时只打 `[CorpseSpawn] multiplayer only` 且不执行);
   物品列表里没点中任何一项时不会执行 (面板静默返回)。
2. **服务端看的是不是当前那份文件**: 服务端 item 日志按**服务器启动时间**命名
   (`logs/YYYY-MM-DD_HH-MM_item.txt`, 如 `2026-09-21_02-13_item.txt`) —— **服务器每次重启都会新建一份**,
   老文件里当然没有本次记录; 按时间挑**最新**那份, 或直接 `tail -f` 跟着看;
3. **包解析失败则既无尸体也无日志**: 服务端 `AddCorpseToMapPacket.parse` 里
   `body = WorldItemTypes.createFromBuffer(b); if (this.body == null) return;` —— 解析失败直接返回,
   后面那条 `corpse +1` 写入**不会执行** (所以"没日志"与"尸体也没落地"常常同时出现);
4. **只在本地看到尸体 = 没上到服务器**: 若客户端日志有 `[CorpseSpawn] …uploaded` 而另一个玩家看不到
   那具尸体, 说明包没被服务端处理 (版本漂移/被拦) —— 此时日志同样不会有 ✅;
5. 对照实验(证明日志链本身通): **建造/制作一次** (消耗背包物品) —— 应写 `container -N`;
   不要用"捡起/丢下物品"当对照: MP 下普通搬运不发包, 一行都不写 (2026-09-21 用户对照测试已证实)。

### 3D.4 规避与选型 (结论)

1. **要零这一行 → 换路线**: 我方生成路线里, **只有尸体生成带 `corpse +1`** —— 钓竿
   (`FishingAction`)、计时生成 (`ISTakeBricks` 原版动作)、陷阱 (`AddExplosiveTrap`)、重掷
   (`clearContainerExplore`)、本地 giveItem/spawnItem、刷弹药 **均不写该行**。
   本地路线与服务端权威动作路线都能把物品送进背包; 但需要"可搜刮的尸体"时, 这条日志无法规避
   (直投 `InvMngGetItem` 已删除: 幽灵物品, 服务端只转发不建物 —— 见 `analysis/计时生成(已实施).md`)。
2. **要低暴露 → 控频 + 场景自洽**: 该行不带**内容信息**(日志只记坐标), 管理员只能进游戏翻尸体
   才知道里面是什么; 若同时有打猎/搬运尸体行为, 行本身有合理解释。**忌高频 + 忌塞离谱物品**。
3. 客户端本地: 该写入点被 `if (GameServer.server)` 包住 → **客户端不写自己的 ItemTracker.log**
   (本地无该功能新痕迹)。

## 四、取证

- 反编译: `zombie/network/anticheats/*.java` (AntiCheat/Speed/HitDamage/HitLongDistance/Player/PlayerUpdate/XPUpdate/PacketValidator), `zombie/network/PacketTypes.java:620-630` (anticheats 注解读取), `zombie/network/packets/character/PlayerPacket*.java` + `packets/hit/*.java` + `packets/vehicle/*.java` (挂载点), `zombie/network/ServerOptions.java:163-174` (默认策略), `zombie/network/GameServer.java:2111-2143` (cmd 日志), `zombie/vehicles/BaseVehicle.java:6698-6817`
- EtherHack 源: `modcore/core/CoreAPI.java:695-712` (秒杀钳值注释), `modcore/fixes/ServerSyncBlocker.lua` (技能本地设置), `modcore/core/LuaMethods.java:211-330` (teleport/spawnItem), `modcore/core/ChatAPI.java`, `lua/fixes/ServerSyncBlocker.lua` (一键启动)
- 游戏 Lua: `client/ISUI/ISInventoryPage.lua:1387` (Refill Container 权限门), `shared/Vehicles/TimedActions/ISStartVehicleEngine.lua`, `server/Vehicles/VehicleCommands.lua` (startEngine)
