# temp / scratch files
temp/
*.tmp

# opencode project-local config (agents/skills, contains private prompt)
.opencode/

# POC 测试文档 (本地使用, 不入仓库)
/tests/刷物品测试指南.md

# 内部规则文件 (仅本地保留, 不入仓库)
/规则.md

# IDE
.idea/
.vscode/
*.iml

# large binaries (jars tracked explicitly if needed)
*.jar.bak
*.jar
!etherhack-src/gradle/wrapper/gradle-wrapper.jar
!etherhack-src/lib/Kahlua.jar
!etherhack-src/lib/fmod.jar
!etherhack-src/lib/org.jar
# zombie.jar 不入 git: 由用户从游戏目录复制 projectzomboid.jar 并改名(见 README 安装步骤)
analysis/pz_jar/projectzomboid.jarluac.out
# stale jar xf extraction (junk)
/EtherHack/
