# EtherHack-3.1.0-B42 物品搜索 + 小地图标记功能 — 可行性分析报告

日期: 2026-08-10
分析对象:
- 游戏: `E:\Steam\steamapps\common\ProjectZomboid\projectzomboid.jar` (B42)
- 外挂: `D:\JavaProject\project_zombies_hackb42` (源项目) / `EtherHack-3.1.0-B42.jar` (成品)
- 本仓库: `D:\JavaProject\project_zombies_analysis` (只读拷贝/分析产物, git 管理)

## 1. 结论(摘要)

**可行, 且可以在不改 Java 代码的前提下完成。** 95% 的工作是改 Lua 界面与新增一个小地图渲染逻辑:

| 需求 | 实现方式 | 依赖 |
|---|---|---|
| 搜索指定物品时出现"显示"按钮 | 在现有 `UIItemTables.lua` 搜索框区域新增按钮, 按过滤到结果时显示 | 纯 Lua |
| 点击后在 minimap 显示该物品位置 | 扫描游戏自带物品注册表 → 得到 `{x, y, count}` 列表 → 在 `UIMap:render()` 中画标记 | 纯 Lua |
| minimap 显示 | 复用现有 `UIMovableMiniMap` (可移动小地图), 标记自动出现在大地图标签页和可移动小地图两处 | 纯 Lua |

游戏 B42 正好提供了两个用于搜索的"物品注册表"(见 §3.1), 避免了遍历整张地图的方格。改造量: 2 个新/改 Lua 文件 + 3 个翻译文件加 3 行 key, 重新打包 jar 或热替换 jar 内 Lua 资源即可。

## 2. 环境核查 (任务1/2/4合规)

- 已建立 git 仓库 `D:\JavaProject\project_zombies_analysis` (本次分析 + 后续实现的版本管理)
- 未修改任何 `E:\Steam\steamapps\common\ProjectZomboid` 下文件 (仅读取与复制)
- 未修改 `D:\JavaProject\project_zombies_hackb42` (仅读取)
- 已复制进本仓库: 游戏 jar、少量解压出来的参考 class (见 `analysis/` 目录)
- 规则文档: `规则.md` (每次开工前重读)

## 2.1 自检取证记录 (2026-08-10, CFR 0.152 反编译, 见 `analysis/decompiled/`)

| 疑点 | 取证结果 | 结论 |
|---|---|---|
| `getProcessItems()` 是否实时维护? | `ItemContainer.AddItem` 两个重载都在加物品时调用 `addToProcessItems` (反编译 `ItemContainer.java:545,617`); 移除走 `processItemsRemove` | ✅ 实时注册表, 扫描即准 |
| 物品世界坐标怎么拿? | `item:getContainer()` → `container:getSquare()` 内部完整解析链: 最外层容器 → 载具部件 → sourceGrid(地面容器) → 父物体方格 → 地面物品方格 (反编译 `ItemContainer.java:3416-3442`); 套袋/套包递归由 `getOutermostContainer` 处理 | ✅ 一行拿坐标 |
| `getContainerX/Y()` 可用吗? | 只是两个裸字段, 全 jar 无任何生产代码调用 `SetContainerPosition`/`setContainerX` | ❌ 弃用, 用 `getSquare()` 方案 |
| 玩家/尸体背包要不要排除? | `container:isInCharacterInventory(getPlayer())` 递归判断 (反编译 `ItemContainer.java:415`) | ✅ 用此过滤 |
| Lua 能调这些方法吗? | PZ 官方 Lua 已用 `getCell():getProcessItems()` (RecipeTests.lua:124)、`item:getContainer()` (ISRemoveItemTool.lua:286); Kahlua2 反射式实例方法派发 | ✅ 全部可直接调 |
| 改完能打包吗? | 已把源项目复制到本仓库 `etherhack-src/` (原项目未动), `gradlew.bat jar` 构建成功, 产物与发行版 **SHA256 逐字节一致** (8D9138EF...16B), Lua 资源均在 jar 内 | ✅ 改造闭环可行 |
| 性能? | 先按 `getFullType()` 字符串比对(零对象调用), 命中后才解析坐标 → 绝大多数物品不命中, 扫描很快; 全命中极端情况另计 | ✅ 点击时扫描可接受 |

结论: **可行性确认无疑**, 无任何阻塞点。下一步实现时仅在 `etherhack-src/` 内改 Lua。

EtherHack jar 结构: fat jar, 内含游戏类 (zombie.jar 合并)、asm/fmod/kahlua/org 依赖, 以及全部 Lua 资源
(`EtherHack/lua/*`), 通过 `Main-Class=EtherHack.Main` 以 agent 方式(ASM 字节码补丁)启动, Lua 由 `EtherLuaMethods`/`EtherLuaManager` 注入并 `requireExtra` 加载。因此**修改 Lua 后只需重新打包** (源项目自带 `build.bat`/gradle), 或直接替换 jar 内的 Lua 条目。

## 3. 游戏侧 API 证据 (projectzomboid.jar 反编译)

### 3.1 物品注册表 — 搜索的数据源 (关键发现)

B42 中, 世界上所有物品都登记在 `IsoCell` 的全局列表里, 游戏自身的 Lua 也在用:

```java
// zombie.iso.IsoCell
public java.util.ArrayList<InventoryItem>                getProcessItems();      // 所有容器内物品 (箱子/冰箱/尸体/玩家背包…)
public java.util.ArrayList<IsoWorldInventoryObject>     getProcessWorldItems();  // 所有地面散落物品
public java.util.List<IsoMovingObject>                  getObjectListForLua();   // 移动物体 (玩家放置的家具等)
```

- `ItemContainer.addItemsToProcessItems()` 在容器创建时把物品注册进去; `InventoryItem.getContainer()`/`ItemContainer.getParent()` 可反查物品所在容器 → 容器父对象 (IsoObject) 有 `getX()/getY()`。
- 地面物品类是 `zombie.iso.objects.IsoWorldInventoryObject extends IsoObject`, 有 `getItem()`、`getX()/getY()`。
- 游戏官方 lua `media/lua/client/Tests/RecipeTests.lua:124` 直接使用 `getCell():getProcessItems()`, 证明该方法在 Lua 层可用。

其他佐证 (Lua 已确认可用):
- `item:getContainer()` — `DebugUIs/ISRemoveItemTool.lua:286,356`
- `getCell()` / `getCell():getZombieList()` / `getCell():getVehicles()` — 现有 `UIMap.lua` 就在用
- `InventoryItem.getFullType()` / `getType()` / `getDisplayName()` — PZ 物品标准方法, Lua 常用
- 坐标解析走 `container:getSquare()` (注意: `getContainerX/Y()` 字段无生产代码维护, 不可用, 见 §2.1)

### 3.2 小地图/大地图坐标换算 (标记定位)

现有 `UIMap.lua` 已实现 `mapAPI:worldToUIX(x,y)` / `worldToUIY(x,y)` 世界坐标→UI坐标,
并已用它画僵尸/载具/玩家标记。物品标记复用同一机制即可, 无需新 API。

## 4. EtherHack 侧结构 (源项目, 只读核查)

### 4.1 物品搜索界面 (添加按钮的位置)

`src/main/resources/EtherHack/lua/components/ui/UIItemTables.lua`:
- 物品创建页签使用 `UIItemTables`, 含两个过滤框: 按名称 (`filterByName`) 和按 ID (`filterById`)
- `ISTextEntryBox.onTextChange` → `UIItemTables.onFilterChange` → 实时过滤列表
- 底部已有一排按钮 (`AddItemX1..X10`), 新按钮照抄此模式即可; 按钮点击回调可拿到当
  前选中的物品 (`self.datas.items[self.datas.selected].item`)

### 4.2 小地图与大地图 (标记绘制的位置)

`src/main/resources/EtherHack/lua/components/ui/UIMap.lua`:
- `UIMap = ISWorldMap:derive(...)`; `UIMap:render()` 末尾追加"物品标记"绘制段即可
- 同一 UIMap 实例被两处复用:
  - 大地图标签页 `EtherMapPanel.lua` (菜单第 6 个按钮, teleport.png)
  - 可移动小地图 `UIMovableMiniMap.lua` (`UIMovableMiniMap.openPanel()`)

### 4.3 Lua 模块加载与翻译

- `EtherHackMenu.lua` 顶部的 `etherModules` 表列出所有模块, `requireExtra(module)` 加载 → 新文件要加进这张表
- 文案走翻译文件 `translations/CN.txt|EN.txt|RU.txt` (`UI_Map_*` / `UI_ItemCreator_*` key 模式), 新增 3 个 key

## 5. 功能设计

### 5.1 交互流程

1. 打开菜单 → 物品创建页签 → 在搜索框输入物品名(如 "大锤" / "sledgehammer")
2. 过滤非空时, 在按钮行出现 **"在地图上显示"** 按钮 (可加 高亮/计数)
3. 点击后:
   a. 用当前过滤条件的结果物品类型列表 (可能多个匹配) 作为目标集合
   b. 扫描 `getCell():getProcessItems()` + `getProcessWorldItems()`, 收集匹配物品的位置
   c. 打开可移动小地图 + 跳转大地图标签页, 在地图上用醒目颜色画标记 (并按 (x,y) 合并计数);
      点击一次 = 刷新一次扫描
4. 再次搜索其他物品并点击, 结果替换

### 5.2 扫描算法 (Lua, 伪代码)

```lua
-- 文件: EtherHack/lua/components/ui/EtherItemSearch.lua (新)
EtherItemSearch = EtherItemSearch or {}
EtherItemSearch.results = nil   -- { {x=.., y=.., count=..}, ... }
EtherItemSearch.target = nil    -- { fullType=bool } 目标集合

function EtherItemSearch.scan(targetTypes)   -- targetTypes: {["Base.Axe"]=true, ...} (已按过滤结果展开)
    local cell = getCell()
    local player = getPlayer()
    local out, key = {}, {}
    local n = 0

    local function addAt(x, y)
        local k = x * 100000 + y
        if key[k] then
            out[key[k]].count = out[key[k]].count + 1
        else
            n = n + 1; key[k] = n
            out[n] = {x = x, y = y, count = 1}
        end
    end

    -- 容器内物品 (先比对类型, 命中才解析坐标 — 保证性能)
    local items = cell:getProcessItems()
    for i = 1, items:size() do
        local item = items:get(i - 1)
        if targetTypes[item:getFullType()] then
            local c = item:getContainer()
            if c ~= nil and not c:isInCharacterInventory(player) then
                local sq = c:getSquare()
                if sq ~= nil then addAt(sq:getX(), sq:getY()) end
            end
        end
    end

    -- 地面散落物品
    local witems = cell:getProcessWorldItems()
    for i = 1, witems:size() do
        local w = witems:get(i - 1)
        local item = w:getItem()
        if item ~= nil and targetTypes[item:getFullType()] then
            addAt(math.floor(w:getX()), math.floor(w:getY()))
        end
    end

    EtherItemSearch.results = out
end
```

说明: `getProcessItems()` 中同一物品可能出现在玩家背包里 → 用 `isInCharacterInventory(player)` 排除; 载具内的物品会标记在载具所在格。

### 5.3 标记绘制 (UIMap.lua 追加)

```lua
-- 在 UIMap:render() 的已有标记段之后:
function UIMap:drawItemSearchMarkers()
    local res = EtherItemSearch.results
    if not res then return end
    for _, p in ipairs(res) do
        local x = self.mapAPI:worldToUIX(p.x, p.y)
        local y = self.mapAPI:worldToUIY(p.x, p.y)
        local size = 125 / self.mapAPI:getWorldScale()
        size = clamp(size, 3, 8)
        -- 醒目色 (如黄色), 带黑边, 与僵尸/载具标记区分
        self:drawRect(x - size, y - size, size * 2 - 1, size * 2 - 1, 1.0, 1.0, 0.8, 0.2)
        self:drawRectBorder(x - size, y - size, size * 2, size * 2, 1, 0, 0, 0)
        if p.count > 1 then
            self:drawTextCentre(tostring(p.count), x, y + size + 2, 1, 1, 0.2, 1.0, UIFont.Small)
        end
    end
end
```

### 5.4 显示按钮 (UIItemTables.lua 追加)

- 在 `createChildren()` 中新增 `UIButton` ("UI_ItemSearch_ShowOnMap"), 默认 `setVisible(false)`
- 在 `onFilterChange` 末尾: 有过滤词且结果数 > 0 → `setVisible(true)`, 记录目标范围 (当前结果集)
- 点击回调: `EtherItemSearch.scan(...)` → `UIMovableMiniMap.openPanel()` (若未开)

### 5.5 文件清单

| 文件 | 动作 |
|---|---|
| `src/main/resources/EtherHack/lua/components/ui/EtherItemSearch.lua` | 新增 (扫描+结果缓存) |
| 同 `ui/UIItemTables.lua` | 修改 (加按钮) |
| 同 `ui/UIMap.lua` | 修改 (渲染标记) |
| `EtherHackMenu.lua` | 修改 (etherModules 表加一行) |
| `translations/CN.txt, EN.txt, RU.txt` | 修改 (新增 key ~3 个) |
| 打包 | `gradlew jar` (`build.bat`), 产物替换 `EtherHack-3.1.0-B42.jar` |

## 6. 局限性 / 风险 (如实说明)

1. **只能显示"已生成区域"的物品**: B42 是流式世界, 客户端只加载玩家附近的区块; 未探索/未生成的区域物品不存在于 `getProcessItems` 中, 不会显示标记。这是客户端数据的天然边界, 无法绕过 (与地图本身只能看已探索区域一致)。
2. **标记是"点击时快照"**: 物品被捡走/搬运后标记不会自动消失, 再次点击按钮刷新即可。若要实时刷新, 可在小地图打开状态下每 N 秒重扫 (性能见下一条), 或监听容器变更事件, 列为后续增强。
3. **性能**: `getProcessItems()` 在大型存档可能上万条, Lua 逐条遍历一次约几十~几百毫秒, 点击时一次性可接受; 若存档巨大, 可改为在 Java 侧 (`EtherLuaMethods`) 用反射实现扫描, 返回 Lua 表, 或加缓存只增量扫描。列为后续优化。
4. **多楼层**: 标记合并到 (x,y) 二维坐标, 多层物品叠加显示计数, 不区分 Z (地图本身也是平面); 如需区分可在计数旁显示楼层。
5. **反作弊/服务器**: 外挂自身有服务器反作弊绕过, 物品扫描不发送任何网络数据, 在单人游戏无风险; 在服务器上视为"透视"类功能由管理员裁决, 不在本报告范围。
6. **游戏版本耦合**: 依赖 `getProcessItems()` 等方法签名, 若 PZ 版本更新改签名需适配; 已提供 `IsoGridSquare:getObjects()/getContainer()` 兜底方案 (遍历周围方格), 见 §7。

## 7. 兜底方案 (若注册表 API 不可用)

逐格扫描: `cell:getGridSquare(x,y,z)` 遍历玩家周围 R 格半径, 对每个方格:
- `square:getObjects()` → 每个 `IsoObject` → `obj:getContainer():getItems()` 或 (地面物品) `obj:getItem()`
- `square:getWorldObjects()` → 地面散落物列表
缺点: 只能扫近距离一格一格来, 数据不全且慢; 仅作 API 不可用时的降级路径, 首选 §5.2。

## 8. 后续步骤 (待确认)

1. (可选) 把 `project_zombies_hackb42` 源码复制进本仓库作为改造基底 (遵守第4条规则)
2. 按 §5 实现 Lua 改动, `gradlew jar` 重新打包
3. 替换 jar 进游戏目录 (规则: 游戏目录只可写入新 jar 属用户操作, 本项目不做)
4. 在游戏内验证, git 提交全部改动

## 9. 功能说明与扫描限制 (2026-08-10 最终实现)

> 注: §2.1/§5.2/§6/§7 中基于 `getProcessItems()` 的结论已被推翻 —— 客户端该注册表恒为空
> (`IsoCell.java:2128` `if (GameClient.client) return`), 最终实现改用已加载方格扫描 (即原 §7 兜底方案)。

### 9.1 使用方式

1. 点击物品搜索 / 添加物品列表里的某项物品 → 底部出现「在地图上显示」按钮 (搜索词非空时按钮存在
   且扫描整个过滤列表; 直接点选物品时按钮扫描该单件物品)。
2. 点击按钮 → 扫描以玩家为中心半径 48 格、玩家所在楼层 ±1 的**已加载区块** → 灰色方块标记命中位置
   (与玩家/僵尸标记同尺寸; 同格多件显示数量)。
3. 标记显示期间每 3 秒自动重扫一次: 物品被捡走/搬动后, 小地图灰色方块会自动消失/更新, 无需手动刷新。
4. 清空搜索词或未选中物品时标记自动清除。

### 9.2 能扫到的容器

- 家具/箱子/书架等 `IsoObject` 上的所有容器 (`getContainerByIndex`)
- 尸体 (尸体的容器也算物件容器)
- 地面散落物品 (`getWorldObjects`)
- 放在地上的包/容器类物品, 及**包内物品** (`getInventory`)
- 车辆后备箱、座位等部件容器 (`getVehicles():toArray()` → 部件 `getItemContainer`)

### 9.3 扫不到的容器 (如实说明)

| 扫不到 | 原因 |
|---|---|
| 玩家自己/其他玩家的背包、身上装备、副手 | 玩家不在方格 `getObjects()` 里, 且刻意排除 |
| 未加载区块内的任何容器 (超出半径或未生成区域) | 客户端只有已加载数据, 无法绕过 (与地图只能看已探索区域同理) |
| 其他玩家刚放入容器的物品 | 客户端数据同步有 1~2 秒延迟, 自动重扫会跟上 |

### 9.4 已知取舍

- 多楼层物品合并到同一个 (x,y) 标记并累计计数, 不区分楼层 (地图是平面的)
- 标记为点选时快照 + 3 秒自动刷新, 极端情况下(超大地图+密集容器)每次重扫几十毫秒, 可接受