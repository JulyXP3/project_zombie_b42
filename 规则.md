# 项目规则 (每次开始工作前必须重读本文件)

## 绝对红线 (违反即失败)

1. **版本管理**: 所有工作产物必须放在 `D:\JavaProject\project_zombies_analysis` 仓库内并用 git 管理。
2. **不得修改 `E:\Steam\steamapps\common\ProjectZomboid` 下的任何文件** (游戏目录):
   - 允许: 读取、复制到本项目;
   - 禁止: 写入/修改/删除/覆盖游戏目录任何文件(包括 jar 替换、写存档等)。
3. **分析对象**: `E:\Steam\steamapps\common\ProjectZomboid\projectzomboid.jar`
   - 目标: 验证能否为 `EtherHack-3.1.0-B42.jar` 添加"搜索物品 → minimap 显示位置"功能。
4. **EtherHack 源项目 `D:\JavaProject\project_zombies_hackb42`**:
   - 允许: 读取、复制到本项目;
   - 禁止: 修改其任何文件 (包括运行 gradle 产生 build 输出)。
   - 要改造时, 必须先整体复制到本项目再做修改。
5. **成品 jar**: 本仓库的 `EtherHack-3.1.0-B42.jar` 是工作副本, 可按需重打包; 若最终要"安装"进游戏目录, 属于用户手动操作, 本项目不代写。
6. 在用户未审查通过之前禁止推送到github仓库

7. **当前工作模式: 红队 (Red Team)**:
   - 本项目当前处于安全对抗阶段。
   - 仅做红队工作: 审计游戏服务端 Lua/Java 漏洞, 以 PoC 功能形式验证漏洞是否真实可行(如「陷阱刷物品」「战利品重掷」选项卡)。防御(蓝队)部分不在本项目范围内。
   - 红队 PoC 功能仅限用于用户自己的服务器/自建测试环境验证, 不得用于任何公共服务器。
   - 用户提出的功能需求用于验证漏洞可行性; 未验证的漏洞不得臆断结论, 验证不成立的结论必须纠正。
   - **工作流: 先实现功能, 再验证** —— 不实现功能就无法验证某方面漏洞是否真实存在; 因此需求一律先以功能形式落地, 经实际运行验证后再下结论, 不得凭静态分析断言结论, 也不得在验证前针对未证实的方向做任何防御性工作(浪费时间)。

## 操作约定

- 临时工具/下载一律放本项目 `temp/` 目录 (已在 .gitignore), 不留垃圾在仓库其他位置。
- 允许自行下载反编译等工具, 允许安装/调用 MCP。
- 大体积二进制 (游戏 jar、依赖 jar) 不入 git, 保留在磁盘本地; 文字产物 (报告、源码、脚本) 必须入 git 并提交。
- 每个阶段结束必须提交一次 git, commit message 用中文描述做了什么。
- **先方案后实现**: 所有分析必须先给出完整方案, 等用户审查通过之后再实现; 未经审查不得动手实现。
- **禁止擅自 push**: 实现功能之后不允许擅自 push, 必须等用户明确说明进行 push 之后再 push。

## 常用路径

- 游戏目录: `E:\Steam\steamapps\common\ProjectZomboid` (只读)
- 游戏 jar: `E:\Steam\steamapps\common\ProjectZomboid\projectzomboid.jar`
- EtherHack 源: `D:\JavaProject\project_zombies_hackb42` (只读)
- 本仓库: `D:\JavaProject\project_zombies_analysis`
- 游戏 Lua (可读): `E:\Steam\steamapps\common\ProjectZomboid\media\lua`
